<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Text Summarizer</title>
  <style>
    body { font-family: sans-serif; padding: 2em; }
    nav a { margin-right: 1em; text-decoration: none; color: #007bff; }
    textarea { width: 100%; height: 200px; margin-top: 1em; }
    button { margin-top: 1em; padding: 0.5em 1em; }
    .result { margin-top: 2em; white-space: pre-wrap; background: #f1f1f1; padding: 1em; }
  </style>
</head>
<body>
  <h1>📝 Text Summarizer</h1>
  <nav>
    <a href="/">Text</a>
    <a href="/summarize-youtube">YouTube</a>
    <a href="/chatbot">Chatbot</a>
  </nav>

  <form method="POST">
    <textarea name="text" placeholder="Paste your text here...">{{ text|default('') }}</textarea><br>
    <button type="submit">Summarize</button>
  </form>

  {% if summary %}
    <div class="result"><strong>Summary:</strong><br>{{ summary }}</div>
  {% endif %}
</body>
</html>

